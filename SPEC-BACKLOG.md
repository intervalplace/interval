# SPEC backlog — sections cited by the engine but never written

0 missing sections, 0 citation sites.
Generated by `node spec-backlog.mjs`. Each entry carries the engine
comment that cites it: the rule is already argued there, it has simply
never been promoted into the constitution.

Writing one of these means: read the block, write the section into
SPEC.md under that exact number, re-run `node spec-conformance.mjs`.

---

